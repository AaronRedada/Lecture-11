#include <iostream>
using namespace std;

int main()
{
    int num;

    cout << "Enter the number for the table for 10:\n";
    cin >> num;

    for (int x = 1; x <= 10; x++) {
        int y = num * x;
        cout << num << " * " << x << " = " << y << endl;
    }
}

